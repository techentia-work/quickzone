export enum VariantQuantityType {
    PACKET = "packet",
    LOOSE = "loose",
}

export enum VariantInventoryType {
    LIMITED = "LIMITED",
    UNLIMITED = "UNLIMITED",
}

export enum VariantStatus {
    AVAILABLE = "AVAILABLE",
    SOLD_OUT = "SOLD_OUT",
}

export enum ProductEatableType {
    VEG = "VEG",
    NON_VEG = "NON_VEG",
    NONE = "NONE",
}

export enum ProductStatus {
    APPROVED = "APPROVED",
    NOT_APPROVED = "NOT_APPROVED",
    REJECTED = "REJECTED",
    PENDING = "PENDING",
}